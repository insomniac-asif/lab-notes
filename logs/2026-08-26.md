# Lab log — 2026-08-26

> drafted by ABL (local model), reviewed by Asif; retrospective evidence audit appended 2026-08-29 · absentbornlabs.org

Went sideways into the vivarium instead. Thirteen commits, almost all rig and render. A full sprite atlas now comes out of a single render, and the rig bends the body rather than pivoting it, which is the difference between a sprite that moves and one that swivels. The identity board is built on the canon render so the design canvas and the runtime agree about what the creature looks like.

Backend: image response modalities, wide strips, and resumable quota runs, so a run that hits a ceiling gets picked up instead of restarted. Two small ones that cost real time: the overlay config tolerates a UTF-8 BOM now, and the README points at a marketplace install path that actually exists.

Four days on the fleet question. I do interesting work whenever the boring work is unbounded, and 'audit 103 processes' is unbounded. The bound holds for tomorrow: exit codes, nothing else.

## Retrospective evidence audit — 2026-08-29

The narrative reports thirteen commits while the public telemetry lists ten selected changes. The three unlisted commits are **UNKNOWN** within this entry. Commit messages support recorded source activity; they do not by themselves prove that the renderer, backend, resume behavior, or install path was deployed and served successfully.

The work is still meaningful: it records a coherent rig/render direction and several engineering fixes. The correction narrows state from “working system” to “reported source changes without a public deployment receipt.”

One environment-variable identifier in the original raw telemetry was generalized to “local credential fallback” during this public-safety repair. No credential value had been published, and the correction is recorded here rather than hidden.

## What did not ship

- no exact thirteen-of-thirteen public commit inventory
- no installed-byte, deployment, or served-output proof
- no claim that the candidate render path was production-ready

## Next

Bind the exact commit set, run the renderer against a frozen fixture, and publish the output plus limitations only after independent readback.

## Raw telemetry

- vivarium: 13 commits
  - Rig: bend the body instead of pivoting it
  - Rig a full atlas from one render
  - Design canvas: identity board built on the canon render
  - Gemini backend: image response modalities, wide strips, resumable quota runs
  - Tolerate a UTF-8 BOM in the overlay config
  - README: real marketplace install path
  - Dragon canon prep: rich prompts, local credential fallback, ideation notes
  - Interaction research + personal-pet flow in the skill
  - Pet interaction + Huma vector design canvas
  - hatch-pet: harvest-and-curate pipeline for local SDXL models
